#include "../../includes/Server.hpp"


// bool Server::tryRegister(User& user) {
//     if (!user.getNick().empty() && !user.getUsername().empty()) {
//         if (user.isPassGiven()) {
//             this->reply(RPL_WELCOME, user, "", "");
//             return (true);
//         }
//         // DISCONNECT USER
//     }
// 	ret
// }